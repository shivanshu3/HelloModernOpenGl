#version 330 core

layout (location = 0) in vec3 vPosition;
layout (location = 1) in vec3 vColor;

uniform vec3 uColor;

out vec3 fColor;

void main()
{
  gl_Position.xyz = vPosition;
  gl_Position.w = 1.0;
  fColor = uColor;
}
